import 'package:flutter/material.dart';

const Color backgroundColor = Color(0xFF010101);
final Color buttonBackgroundColor = Colors.grey.shade900;
const Color primaryColor = Color(0xFF6AD13A);

var operators = ['( )', '%', '/', '*', '-', '+'];
